[build]
builder = "nixpacks"

[deploy]
startCommand = "python railway_simple.py"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PYTHONPATH = "."
FLASK_ENV = "production"
