ased on the provided memories, I can explain how the Telegram bot was implemented:

کتابخانه انتخابی: از کتابخانه pyTelegramBotAPI استفاده شد. این کتابخانه به جای python-telegram-bot انتخاب شد چون:
ساده‌تر و پایدارتر است
مشکل وابستگی به ماژول imghdr را ندارد
پیاده‌سازی توابع ارسال پیام در آن راحت‌تر است
تنظیمات اصلی:
توکن ربات: 7429658178:AAFc8hfXeog2Cu8EWOcXQbMc5Nn-q-f1ePk
شناسه چت: 722627622
این اطلاعات در فایل config.py ذخیره شده‌اند
زمان‌بندی ارسال پیام‌ها:
گزارش‌های سریع: هر ۱۰ دقیقه
گزارش‌های تفصیلی: هر ۱ ساعت
این فاصله‌گذاری برای جلوگیری از محدودیت API انتخاب شده
اجرای خودکار:
یک فایل run_bot.sh برای اجرای ربات ایجاد شده
یک اپلیکیشن StartSalamBot.app در Startup Items سیستم قرار داده شده که باعث می‌شود ربات به صورت خودکار با روشن شدن سیستم اجرا شود
این ساختار ساده و کارآمد باعث شده که ربات به خوبی کار کند و به راحتی قابل نگهداری باشد.

Feedback submitted
Me

kodhash ro koja neveshti?

Let me check the project structure to show you where the Telegram bot code is located.

Searched filesystem
146 results
Searchedforfilesin~/Downloads/SalamMessenger
بر اساس ساختار پروژه، کدهای اصلی ربات تلگرام در چند فایل کلیدی قرار دارند:

فایل اصلی: main.py در مسیر اصلی پروژه
کدهای تنظیمات: در پوشه crypto_bot/config.py
کد ارسال پیام: در پوشه crypto_bot/message_sender.py
بذارید محتوای message_sender.py رو که کد اصلی ارتباط با تلگرام توش هست رو ببینم:

Analyzed

message_sender.py
حالا بذارید main.py رو هم ببینم که ببینیم چطور از این کد استفاده میشه:

Analyzed

main.py
خب، کل ساختار کد به این صورته:

message_sender.py:
کد اصلی ارتباط با تلگرام اینجاست
از کتابخانه telebot (pyTelegramBotAPI) استفاده میکنه
یک تابع send_message داره که پیام‌ها رو به تلگرام میفرسته
main.py:
فایل اصلی برنامه که همه چیز رو مدیریت میکنه
از scheduled_reports برای ارسال گزارش‌های دوره‌ای استفاده میکنه
یک حلقه بی‌نهایت داره که برنامه رو زنده نگه میداره
config.py:
تنظیمات مهم مثل توکن ربات و شناسه چت اینجا ذخیره شدن
تنظیمات گزارش‌ها هم اینجاست
run_bot.sh:
اسکریپتی که برنامه رو اجرا میکنه
به صورت خودکار با روشن شدن سیستم اجرا میشه
این ساختار ساده و مدولار باعث میشه که:

کد تمیز و قابل فهم باشه
هر بخش مسئولیت خودش رو داره
نگهداری و تغییر کد راحت باشه